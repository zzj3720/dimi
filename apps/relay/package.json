{
  "name": "@dimi-agent/relay",
  "version": "0.1.0",
  "private": true,
  "description": "Opaque WebSocket relay for remote agent runtime connections.",
  "license": "MIT",
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "build": "wrangler deploy --dry-run --outdir dist",
    "typecheck": "wrangler types --include-runtime=false --check && tsc -p tsconfig.json --noEmit",
    "test": "vitest run --config vitest.config.ts"
  },
  "dependencies": {
    "@dimi-agent/remote": "workspace:^"
  },
  "devDependencies": {
    "@cloudflare/vitest-pool-workers": "0.20.0",
    "@cloudflare/workers-types": "5.20260731.1",
    "@types/ws": "^8.18.0",
    "vitest": "4.1.4",
    "wrangler": "4.117.0",
    "ws": "^8.18.0"
  }
}

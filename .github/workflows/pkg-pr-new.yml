name: pkg.pr.new

on:
  pull_request:
    types: [opened, synchronize, reopened, ready_for_review]

permissions:
  contents: read

concurrency:
  group: ${{ github.workflow }}-${{ github.event.pull_request.number }}
  cancel-in-progress: true

jobs:
  publish:
    name: Publish Dimi preview
    runs-on: ubuntu-latest
    if: github.repository_owner == 'MoonshotAI' && github.event.pull_request.draft == false

    steps:
      - name: Checkout
        uses: actions/checkout@v6

      - name: Setup pnpm
        uses: pnpm/action-setup@v6

      - name: Setup Node.js
        uses: actions/setup-node@v6
        with:
          node-version-file: .nvmrc
          cache: pnpm

      - name: Install dependencies
        run: pnpm install --frozen-lockfile

      - name: Build package dependencies
        run: pnpm run build:packages

      - name: Build Dimi web assets
        run: pnpm --filter @dimi-agent/dimi-web run build

      - name: Generate Dimi built-in catalog
        shell: bash
        run: |
          CATALOG_FILE="$RUNNER_TEMP/dimi-built-in-catalog.json"
          node apps/dimi/scripts/update-catalog.mjs --out "$CATALOG_FILE"
          echo "DIMI_CODE_BUILT_IN_CATALOG_FILE=$CATALOG_FILE" >> "$GITHUB_ENV"

      - name: Build Dimi package
        run: pnpm --filter @dimi-agent/cli run build

      - name: Publish pkg.pr.new preview
        run: pnpm exec pkg-pr-new publish --bin --commentWithSha --packageManager=pnpm,npm --no-template './apps/dimi'

---
"@moonshot-ai/kimi-code": patch
---

Disable automatic upgrades and remote banners until this source repository has its own release channel, so a 0.1.x build cannot install or advertise an unrelated pre-fork release.

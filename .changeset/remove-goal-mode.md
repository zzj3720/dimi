---
"@dimi-agent/cli": minor
---

Remove goal mode: the `/goal` command, the goal tools (CreateGoal, GetGoal, SetGoalBudget, UpdateGoal), the session goal API, and the `GET /sessions/{id}/goal` route are gone.

---
"@dimi-agent/cli": minor
---

Announce the `AllDone` completion protocol in the base system prompt and require agents to review every tool-free response and explicitly call `AllDone` before ending, while refusing completion until background tasks have settled.
